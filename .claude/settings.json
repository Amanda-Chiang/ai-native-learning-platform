{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in *src/*.ts|*src/*.tsx) npm run typecheck ;; esac; } 2>/dev/null || true",
            "statusMessage": "Typechecking..."
          }
        ]
      }
    ]
  }
}
