# Visual regression tests

No Concept Atlas UI exists yet (`src/app/page.tsx` is still the base
scaffold), so there's nothing real to snapshot. Add specs here once the
atlas renderer lands, using `tests/fixtures/concept-atlas-demo.json`.
Required screenshots are listed in
`.claude/skills/concept-atlas/references/testing.md`: whole-course atlas,
expanded unit, focused concept, weak-relationship state, and the
mobile/tablet breakpoint.
